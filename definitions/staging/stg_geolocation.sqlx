config {
  type: "view",
  schema: "staging",
  description: "Cleaned version of customer table"
}

with source as (
    select * from ${ref("olist_geolocation_dataset")}
),

renamed as (
    select *
    from source
)

select * from renamed